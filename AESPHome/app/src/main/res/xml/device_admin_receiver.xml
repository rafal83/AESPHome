<?xml version="1.0" encoding="utf-8"?>
<!-- lockNow() (the only DevicePolicyManager call this app makes) needs no specific
     uses-policy — any active device admin may call it. -->
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-policies>
  </uses-policies>
</device-admin>
