<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />
  <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

  <!-- Bluetooth LE passive scan (proxy): pre-12 needs location to receive scan results at
       all; 12+ uses the dedicated scan permission instead, marked neverForLocation since
       this app never uses BLE results to derive location. -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />

  <!-- Screen brightness / orientation: system-wide control via Settings.System requires this
       special app-op permission, granted through its own Settings screen (see Permissions). -->
  <uses-permission android:name="android.permission.WRITE_SETTINGS" />

  <!-- button.screen_wake's short, self-releasing WakeLock. -->
  <uses-permission android:name="android.permission.WAKE_LOCK" />

  <!-- text_sensor.foreground_app: another special app-op permission, granted through its own
       Settings screen — flagged for the linter since it's normally Play-Store-restricted;
       this app only reads usage stats locally to report the current foreground app, never
       uploads them anywhere but to the user's own Home Assistant. -->
  <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
      tools:ignore="ProtectedPermissions" />

  <!-- select.launch_app's picker needs to see launchable apps without the heavily-restricted
       QUERY_ALL_PACKAGES permission. -->
  <queries>
    <intent>
      <action android:name="android.intent.action.MAIN" />
      <category android:name="android.intent.category.LAUNCHER" />
    </intent>
  </queries>

  <!-- Auto-update (sensors/auto_update.kt): lets this app launch the system package installer
       for an update APK it downloaded itself — the user still has to tap "Install" on
       Android's own confirmation screen unless the app is also Device Owner (no in-app grant
       flow for that — see utils.kt's isDeviceOwner() and FAQ.md), in which case the same
       download installs with zero taps via PackageInstaller. -->
  <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

  <application
      android:label="ÆSPHome"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:theme="@style/Theme.AESPHome">

    <activity
        android:name=".MainActivity"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
        android:name=".PermissionsActivity"
        android:exported="false"
        android:label="Permissions"
        android:parentActivityName=".MainActivity" />

    <activity
        android:name=".AppLauncherSettingsActivity"
        android:exported="false"
        android:label="Allowed Apps"
        android:parentActivityName=".MainActivity" />

    <service
        android:name=".AESPHomeService"
        android:exported="false"
        android:foregroundServiceType="mediaPlayback|camera|microphone|connectedDevice" />

    <receiver
        android:name=".BootReceiver"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>

    <receiver
        android:name=".AESPHomeDeviceAdminReceiver"
        android:exported="true"
        android:permission="android.permission.BIND_DEVICE_ADMIN">
      <meta-data
          android:name="android.app.device_admin"
          android:resource="@xml/device_admin_receiver" />
      <intent-filter>
        <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
      </intent-filter>
    </receiver>

    <service
        android:name=".TouchAccessibilityService"
        android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
        android:exported="true">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
          android:name="android.accessibilityservice.config"
          android:resource="@xml/accessibility_service_config" />
    </service>

    <!-- Receives PackageInstaller's async commit result for a Device Owner silent install
         (auto_update.kt) — success/failure, or a fallback confirmation UI if a specific
         device doesn't fully honor the silent-install privilege. Not exported: only this
         app's own PendingIntent (passed to PackageInstaller.Session.commit()) ever targets it. -->
    <receiver
        android:name=".AESPHomeUpdateInstallReceiver"
        android:exported="false" />

    <!-- Hands the downloaded update APK (auto_update.kt) to the system installer as a
         content:// URI instead of a raw file:// path — required on API 24+. -->
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths" />
    </provider>

  </application>
</manifest>
